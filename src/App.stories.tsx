import type { Meta } from '@storybook/react'
import App from './App'
const meta = {
  title: 'App',
  tags: ['autodocs'],
  component: App
} satisfies Meta<typeof App>

export default meta

export const Default = {}
